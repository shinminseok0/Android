<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShinTech"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".FindPasswordActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".MemberMainActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".PhoneCreateActivity" />
        <activity android:name=".ImagePickerActivity" />
        <activity android:name=".InterestActivity" />
        <activity android:name=".ReviewPhoneSelectActivity" />
        <activity android:name=".ReviewListActivity" />
        <activity android:name=".OrderHistoryActivity" />
        <activity android:name=".TotalSalesActivity" />
        <activity android:name=".CouponListActivity" />

        <!-- 결제 완료 후 돌아오기 위한 딥링크 설정 -->
        <activity
            android:name=".PaymentActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="shintech" android:host="payment" />
            </intent-filter>
        </activity>

        <!-- 댓글 조회 관련 액티비티 등록 -->
        <activity android:name=".CommentPhoneSelectActivity" />
        <activity android:name=".CommentListActivity" />

    </application>

</manifest>
